---
'@kybernetes/web': patch
---

Cruise starfield now streams while the hull holds mid-leg: the viewport latches the departure bearing and drives scroll from leg progress (ramping up off departure, easing and inverting through the braking half, frozen on flameout) instead of going static with the frozen hull. The debug canvas lays the shared-berth stations out side by side with a display-only spread so all four hubs stay inspectable.
