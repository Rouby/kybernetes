---
"@kybernetes/sim-core": patch
---

Gate plot fuel on the flown leg window instead of the nominal lane table, so the server accepts exactly what the client projects and flyable legs are no longer rejected.
