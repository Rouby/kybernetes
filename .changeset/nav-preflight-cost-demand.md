---
"@kybernetes/server": patch
"@kybernetes/web": patch
---

Nav bridge pass: pre-flight readiness checklist on a dedicated top-left side panel with LOAD FUEL / SPOOL bridge actions (server allows fuel handling at the nav console and pure spool latching from the bridge), star chart trip-cost and low-stores warnings on course preview, and target-port cargo demand table replacing the static haul hint.
