---
"@kybernetes/web": patch
---

Seamless dock crossings: prediction holds one auth frame on frame changes so the pawn never flashes across the map, and fog-of-war outside the home grid reads as explored instead of void so far stations draw their interiors.
