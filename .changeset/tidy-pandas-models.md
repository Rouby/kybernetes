---
"@kybernetes/sim-core": patch
---

Replicate static console models per hub frame (skipped where a variant moved the room) so foreign stations draw their appliances, and only ship consoles ride the vessel offset.
