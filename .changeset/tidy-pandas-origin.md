---
"@kybernetes/web": patch
---

Seed the client origin map with static station origins, offset the docking umbilical guides by the active dock mouth, and render station hull backing plates across visible hubs so far stations render completely in place without visual disconnect.
