{
  "name": "@kybernetes/sim-core",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:browser": "vitest run --config ./vitest.browser.config.ts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "@kybernetes/air-sim": "workspace:*",
    "@kybernetes/protocol": "workspace:*"
  },
  "devDependencies": {
    "@kybernetes/tsconfig": "workspace:*",
    "@vitest/browser": "5.0.0",
    "@vitest/browser-playwright": "5.0.0",
    "playwright": "1.62.1",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
